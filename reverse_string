def reverse_text(the_string):
    start = 0
    end = len(the_string) - 1
    while end >= 0:
        yield the_string[end]
        end -= 1


for char in reverse_text("step"):
    print(char, end='')
